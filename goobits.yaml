# Goobits configuration for STT (Speech to Text)
# This file replaces the old setup-config.yaml and defines everything needed for the build command

# Setup fields for setup.sh
package_name: "goobits-stt"
command_name: "stt"
display_name: "STT - Speech to Text"
description: "Real-time speech transcription powered by Whisper"

# CLI generation configuration
cli_output_path: "src/goobits_stt/cli.py"
hooks_path: "src/goobits_stt/app_hooks.py"

python:
  minimum_version: "3.8"
  maximum_version: "3.13"

dependencies:
  required:
    - git
    - pipx
  optional:
    - ffmpeg  # For audio processing
    - portaudio  # For microphone input (system package)
  development:
    - ruff    # Modern Python linter and formatter
    - mypy    # Static type checker
    - black   # Code formatter
    - pytest  # Testing framework

installation:
  pypi_name: "goobits-stt"
  development_path: "."
  extras:
    python: ["audio"]  # Python extras including pyaudio
    apt: ["libportaudio2-dev", "ffmpeg"]  # System packages for audio support

shell_integration:
  enabled: false
  alias: "stt"

validation:
  check_api_keys: false
  check_disk_space: true
  minimum_disk_space_mb: 500

messages:
  install_success: |
    STT has been installed successfully!
    You can now use 'stt' from your terminal.

    ⚠️  IMPORTANT: For audio device support, install with:
       pipx install goobits-stt[audio] --force

    Quick start:
      stt listen              # Record and transcribe once
      stt live                # Interactive conversation mode
      stt serve               # Start WebSocket server

  install_dev_success: |
    STT has been installed in development mode!
    ✅ Your local changes will be reflected immediately - no reinstalling needed!

    ⚠️  IMPORTANT: For audio device support, install with:
       cd /workspace/stt && pipx install -e .[audio] --force

    Development workflow:
      - Edit code in src/goobits_stt/ directory
      - Test immediately with: stt listen
      - Run tests with: ./test.py
      - Format code with: ruff format src/goobits_stt/
      - Check types with: mypy src/goobits_stt/

    💡 No need to run ./setup.sh upgrade after code changes!

  upgrade_success: |
    STT has been upgraded successfully!
    Check out the latest features with: stt --version

  uninstall_success: |
    STT has been uninstalled.
    Thank you for using STT!

# CLI fields for cli.py generation
cli:
  name: "GOOBITS STT CLI"
  version: "1.0.0"
  display_version: true
  tagline: "Speech to Text"
  description: "Real-time speech transcription powered by Whisper"
  icon: "🎤"
  enable_recursive_help: true
  enable_help_json: true

  header_sections:
    - title: "💡 Quick Start"
      items:
        - { item: "stt listen", desc: "Record once and transcribe", style: "example" }
        - { item: "stt live", desc: "Interactive conversation mode", style: "example" }
        - { item: "stt serve", desc: "Start WebSocket server", style: "example" }
        - { item: "stt models", desc: "List available Whisper models", style: "example" }

    - title: "🔑 Initial Setup"
      items:
        - { item: "1. Check status", desc: "stt status", style: "setup" }
        - { item: "2. Select model", desc: "stt config set model base", style: "setup" }
        - { item: "3. Start listening", desc: "stt listen", style: "setup" }

  footer_note: "📚 For detailed help on a command, run: [color(2)]stt [COMMAND][/color(2)] [#ff79c6]--help[/#ff79c6]"

  command_groups:
    - name: "Recording Modes"
      commands: ["listen", "live"]
    - name: "Server & Processing"
      commands: ["serve"]
    - name: "System"
      commands: ["status", "models"]
    - name: "Configuration"
      commands: ["config"]
  
  commands:
    listen:
      desc: "Record once and transcribe"
      icon: "🎙️"
      options:
        - name: "model"
          short: "m"
          type: "str"
          desc: "Whisper model size"
          choices: ["tiny", "base", "small", "medium", "large"]
          default: "base"
        - name: "language"
          short: "l"
          type: "str"
          desc: "Language code (e.g., en, es, fr)"
        - name: "device"
          short: "d"
          type: "str"
          desc: "Audio input device name"
        - name: "hold-to-talk"
          type: "str"
          desc: "Hold key to record (e.g., space, f8)"
        - name: "no-formatting"
          type: "bool"
          desc: "Disable text formatting"
          default: false
        - name: "sample-rate"
          type: "int"
          desc: "Audio sample rate in Hz"
          default: 16000
        - name: "json"
          type: "flag"
          desc: "Output as JSON"
        - name: "debug"
          type: "flag"
          desc: "Enable debug logging"
        - name: "config"
          type: "str"
          desc: "Path to config file"

    live:
      desc: "Interactive conversation mode"
      icon: "💬"
      options:
        - name: "model"
          short: "m"
          type: "str"
          desc: "Whisper model size"
          choices: ["tiny", "base", "small", "medium", "large"]
          default: "base"
        - name: "language"
          short: "l"
          type: "str"
          desc: "Language code (e.g., en, es, fr)"
        - name: "device"
          short: "d"
          type: "str"
          desc: "Audio input device name"
        - name: "tap-to-talk"
          type: "str"
          desc: "Key to tap for push-to-talk (e.g., f8)"
        - name: "no-formatting"
          type: "bool"
          desc: "Disable text formatting"
          default: false
        - name: "sample-rate"
          type: "int"
          desc: "Audio sample rate in Hz"
          default: 16000
        - name: "json"
          type: "flag"
          desc: "Output as JSON"
        - name: "debug"
          type: "flag"
          desc: "Enable debug logging"
        - name: "config"
          type: "str"
          desc: "Path to config file"

    serve:
      desc: "Start transcription server"
      icon: "🌐"
      options:
        - name: "port"
          short: "p"
          type: "int"
          desc: "Port to run server on"
          default: 8769
        - name: "host"
          short: "h"
          type: "str"
          desc: "Host to bind to"
          default: "0.0.0.0"
        - name: "debug"
          type: "flag"
          desc: "Enable debug logging"
        - name: "config"
          type: "str"
          desc: "Path to config file"

    status:
      desc: "Check system and device status"
      icon: "✅"
      options:
        - name: "json"
          type: "flag"
          desc: "Output as JSON"

    models:
      desc: "List available Whisper models"
      icon: "📦"
      options:
        - name: "json"
          type: "flag"
          desc: "Output as JSON"

    config:
      desc: "Manage configuration"
      icon: "⚙️"
      subcommands:
        show:
          desc: "Show all configuration"
          options:
            - name: "json"
              type: "flag"
              desc: "Output as JSON"
        get:
          desc: "Get configuration value"
          args:
            - name: "key"
              desc: "Configuration key"
              required: true
        set:
          desc: "Set configuration value"
          args:
            - name: "key"
              desc: "Configuration key"
              required: true
            - name: "value"
              desc: "Configuration value"
              required: true

  config:
    rich_help_panel: true
    show_metavars_column: false
    append_metavars_help: true
    style_errors_suggestion: true
    max_width: 120